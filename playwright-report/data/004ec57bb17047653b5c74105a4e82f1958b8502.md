# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: real-flow.spec.ts >> Real E2E Flow >> should register, login, and perform basic actions
- Location: tests\e2e\real-flow.spec.ts:7:3

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /.*#\/(dashboard|login)/
Received string:  "http://localhost:3000/#/register"
Timeout: 15000ms

Call log:
  - Expect "toHaveURL" with timeout 15000ms
    33 × unexpected value "http://localhost:3000/#/register"

```

```yaml
- heading "Contazze" [level=1]
- paragraph: Teste grátis por 7 dias. Sem compromisso.
- heading "Crie sua conta" [level=2]
- paragraph: Preencha seus dados para começar
- text: "Campos obrigatórios faltando: admin_name"
- textbox "Razão Social ou Nome Completo": QA Auto Tester
- img
- textbox "CPF ou CNPJ (somente números)": "00000000000"
- textbox "WhatsApp / Telefone": "11999999999"
- img
- textbox "E-mail de Login": qa_auto_1779718694694@example.com
- img
- textbox "Senha": Password123!
- textbox "Confirmar": Password123!
- button "Começar Avaliação Gratuita"
- paragraph: Ao se cadastrar, você concorda com nossos termos de uso.
- paragraph:
  - text: Já tem uma conta?
  - link "Fazer login":
    - /url: "#/login"
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Real E2E Flow', () => {
  4  |   const testEmail = `qa_auto_${Date.now()}@example.com`;
  5  |   const password = 'Password123!';
  6  | 
  7  |   test('should register, login, and perform basic actions', async ({ page }) => {
  8  |     test.setTimeout(60000); // give it more time for API calls
  9  |     
  10 |     // 1. Navegar para Registro
  11 |     await page.goto('/#/register');
  12 |     await expect(page.getByText('Crie sua conta', { exact: true })).toBeVisible();
  13 |     
  14 |     // 2. Preencher formulário de registro (usando getByRole/getByLabel de acordo com o snapshot)
  15 |     await page.getByRole('textbox', { name: 'Razão Social ou Nome Completo' }).fill('QA Auto Tester');
  16 |     await page.getByRole('textbox', { name: 'CPF ou CNPJ' }).fill('00000000000');
  17 |     await page.getByRole('textbox', { name: 'WhatsApp' }).fill('11999999999');
  18 |     await page.getByRole('textbox', { name: 'E-mail de Login' }).fill(testEmail);
  19 |     await page.getByRole('textbox', { name: 'Senha' }).first().fill(password);
  20 |     await page.getByRole('textbox', { name: 'Confirmar' }).fill(password);
  21 |     
  22 |     // 3. Submeter
  23 |     await page.getByRole('button', { name: 'Começar Avaliação Gratuita' }).click();
  24 |     
  25 |     // Esperar a conclusão e login
> 26 |     await expect(page).toHaveURL(/.*#\/(dashboard|login)/, { timeout: 15000 });
     |                        ^ Error: expect(page).toHaveURL(expected) failed
  27 | 
  28 |     if (page.url().includes('#/login')) {
  29 |       await page.getByPlaceholder('seu@email.com').fill(testEmail);
  30 |       await page.getByPlaceholder('••••••••').fill(password);
  31 |       await page.getByRole('button', { name: 'Entrar' }).click();
  32 |     }
  33 | 
  34 |     // Esperar dashboard carregar (ou setup)
  35 |     await page.waitForTimeout(5000);
  36 |     
  37 |     // Se a empresa ainda não existir, pode haver um modal ou um estado vazio. 
  38 |     // Vamos validar que a tela não quebrou (ex: erro crítico do React)
  39 |     const hasHeader = await page.getByRole('heading', { name: 'Contazze' }).isVisible();
  40 |     expect(hasHeader).toBeTruthy();
  41 |   });
  42 | });
  43 | 
```